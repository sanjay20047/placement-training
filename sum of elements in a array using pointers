#include<iostream>
using namespace std;
int sum(int *arr,int n){
    int s;
    for(int i=0;i<n;i++){
        s=s+*(arr+i);
    }
    return s;
    
}
int main(){
    int arr[]={2,4};
    int n=sizeof(arr)/sizeof(arr[0]);
    cout<<sum(arr,n);
}
